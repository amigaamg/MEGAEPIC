'use client';

// Constitutional theme is applied via CSS variables in globals.css.
// No runtime theme switching needed — sky blue + white is the permanent theme.

export function DashboardThemeInjector() {
  return null;
}

export function ThemeSwatches() {
  return null;
}
