"use client";
export default function PrintExportPanel({ patientId, patient, doctorId, doctorName, modal, onClose }: { patientId: string; patient?: any; doctorId?: string; doctorName?: string; modal?: boolean; onClose?: () => void }) {
  return <div style={{ display: "none" }} />;
}
