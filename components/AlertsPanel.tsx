"use client";
export function AlertsPanel({ patientId, doctorId, onCountChange }: { patientId: string; doctorId?: string; onCountChange?: (n: number) => void }) {
  return <div style={{ display: "none" }} />;
}
